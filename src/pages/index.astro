---
// Redirects from / to /en (the default locale with prefix)
// This file is required by Astro's i18n routing when prefixDefaultLocale: true.
// The Vercel rewrite in vercel.json handles this more efficiently in production deployments.

export const prerender = false; // Ensure this runs server-side for the redirect

// Redirect directly to the default locale's prefixed path.
return Astro.redirect('/en');
---
<!-- This content will not be rendered due to the redirect -->
<p>Redirecting...</p>
